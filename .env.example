# SQLite 기본값으로 돌아갑니다. (아무 설정도 필요 없음)
# Postgres 사용 시 아래처럼 DATABASE_URL만 지정
# DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME

DJANGO_DEBUG=True
DJANGO_SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=127.0.0.1,localhost
